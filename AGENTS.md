# Project specific instructions

## Python CLI tool for portfolio tracking

- package manager: **uv**
- package metadata is in [pyproject.toml](pyproject.toml)
- project structure: [src](src)
- DuckDB database is used for data storage
- pure JSON response
- Scripts are located in [scripts](scripts) directory

## Style

- Variables use `snake_case`
- Constants use `UPPER_SNAKE_CASE`
- Classes use `PascalCase`
- Methods use `snake_case`
- Functions use `snake_case`

## Documentation

- Public documentation lives in [docs](docs)
- Keep repo guidance concise and public-safe
