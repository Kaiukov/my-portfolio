You are working on PR #296 (`feat/openai-tunnel`) — a Draft PR that adds an OpenAI tunnel MCP server and API CORS support to the portfolio-ts project.

## What's been done
- A stdio MCP server entrypoint (`bun run mcp`) in `portfolio-ts/src/mcp/server.ts`
- Read/write MCP tools registered through the tunnel-friendly server
- Opt-in API CORS support in `portfolio-ts/src/api/server.ts`
- MCP docs/status references updated

## What needs to be done

1. **CI is failing** — check the test run and fix whatever is broken
2. **Review the implementation** — make sure the MCP server properly registers all read/write tools and the CORS setup works
3. **Run `bun run typecheck`** and fix any type errors
4. **Run `bun test`** and make sure all tests pass

## Constraints
- Do NOT merge or close the PR — keep it as Draft
- Do NOT rebase or force-push
- Push fixes to the same `feat/openai-tunnel` branch
- Keep explanations minimal, focus on code
- Work iteratively: fix → verify → push

Start by checking the CI failure, then work through the issues.
